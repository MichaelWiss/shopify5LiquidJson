<section class="hero">
  {% assign fallback_hero = 'https://images.unsplash.com/photo-1600210492486-724fe5c67fb0?w=1600&q=90' %}
  {% if section.settings.image != blank %}
    {{ section.settings.image | image_url: width: 3200 | image_tag: loading: 'eager', class: 'hero-image', sizes: '100vw' }}
  {% else %}
    <img src="{{ fallback_hero }}" alt="{{ section.settings.heading | default: 'Refinements' | escape }}" class="hero-image" loading="eager">
  {% endif %}
  <div class="hero-overlay"></div>
  {% if section.settings.heading != blank %}
    <h1 class="hero-title" {% if section.settings.enable_scroll_fade %}data-scroll-fade{% endif %}>{{ section.settings.heading }}</h1>
  {% endif %}
</section>

{% schema %}
{
  "name": "Hero full bleed",
  "tag": "section",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Hero image"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Refinements"
    },
    {
      "type": "checkbox",
      "id": "enable_scroll_fade",
      "label": "Enable scroll fade effect",
      "default": true,
      "info": "Logo fades and moves as user scrolls"
    }
  ],
  "presets": [
    {
      "name": "Hero full bleed"
    }
  ]
}
{% endschema %}
