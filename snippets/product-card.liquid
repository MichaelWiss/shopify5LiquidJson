<article class="product-card" {% if product.available == false %}data-product-sold-out="true"{% endif %}>
  <a class="product-card__link" href="{{ product.url }}">
    <div class="product-card__media">
      {% liquid
        assign loading = 'lazy'
        if lazy_load == false
          assign loading = 'eager'
        endif
      %}
      {% render 'responsive-image', image: product.featured_media, width: 700, loading: loading, class: 'product-card__image', sizes: '(min-width: 1200px) 33vw, (min-width: 768px) 50vw, 100vw', placeholder_type: 'product-1', placeholder_class: 'product-card__placeholder' %}
    </div>

    <div class="product-card__meta">
      <div class="product-card__heading">
        <h3 class="product-card__title">{{ product.title }}</h3>
        {% if product.tags contains 'new' %}
          <span class="product-card__badge">New</span>
        {% endif %}
      </div>
      <p class="product-card__price">
        {% if product.price_varies %}
          From {{ product.price_min | money }}
        {% else %}
          {{ product.price | money }}
        {% endif %}
      </p>
    </div>
  </a>
</article>
